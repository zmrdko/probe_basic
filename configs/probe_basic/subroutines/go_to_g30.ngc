o<go_to_g30> sub

M73

o100 if [#<_abs_x> NE #5181 OR #<_abs_y> NE #5182]
    G91 G28 Z0 (go home in Z only)
    G91 G30 X0 Y0 (go to G30 in X and Y)
o100 endif

G90 G30 (go G30 in Z)

o110 if [EXISTS[#<_ini[traj]axes>]]
    #<geometry> = #<_ini[traj]axes>
o110 endif

o120 if [#<geometry> EQ 4]
    G53 G0 A#5184
o120 else if [#<geometry> EQ 5]
    G53 G0 A#5184 C#5186
o120 endif

o<go_to_g30> endsub

M2
